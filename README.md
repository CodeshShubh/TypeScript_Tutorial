# TypeScript_Tutorial

-> initilized TypeScript
(1) ->initilized typeScript ->
    -> npm i -g typescript
    -> tsc -init
    -> change(tscconfig.json) ->target
                              ->rootDir
                              ->outDir

